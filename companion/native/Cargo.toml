[package]
name = "hands-return-bridge"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Hands Return Bridge companion native host and pairing CLI"
publish = false

[dependencies]
rusqlite = { version = "0.40", features = ["bundled"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10"

[dev-dependencies]
tempfile = "3.16"
